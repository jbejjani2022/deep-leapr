{"used_features": ["def feature(text: str) -> float:\n    \"Entropy of the character distribution in the text\"\n    from collections import Counter\n    import math\n    char_count = Counter(text)\n    total_chars = sum(char_count.values())\n    return -sum((count / total_chars) * math.log(count / total_chars, 2) for count in char_count.values() if count > 0)\n\n", "def feature(text: str) -> float:\n    \"Ratio of specific keywords related to the context\"\n    keywords = ['AYilunGOT', 'GOATYilun', 'YilunGOAT', 'ZZZZZZZZZZ']\n    keyword_count = sum(1 for keyword in keywords if keyword in text)\n    return float(keyword_count)\n", "def feature(text: str) -> float:\n    \"Count of specific phrase occurrences in the text\"\n    phrase = 'YilunGOAT'\n    count = text.count(phrase)\n    return float(count)\n", "def feature(text: str) -> float:\n    'Entropy of character distribution in the text'\n    from collections import Counter\n    import math\n    if len(text) == 0:\n        return 0.0\n    char_counts = Counter(text)\n    probabilities = [count / len(text) for count in char_counts.values()]\n    entropy = -sum(p * math.log2(p) for p in probabilities)\n    return entropy\n\n", "def feature(text: str) -> float:\n    'Proportion of specific repeating characters in the text'\n    total_length = len(text)\n    if total_length == 0:\n        return 0.0\n    count_repeating = text.count('Z')\n    return count_repeating / total_length\n\n", "def feature(text: str) -> float:\n    \"Ratio of the most frequent character to the total number of characters\"\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    char_counts = {}\n    for char in text:\n        if char.isalnum():\n            char_counts[char] = char_counts.get(char, 0) + 1\n    most_frequent = max(char_counts.values(), default=0)\n    return most_frequent / total_chars\n\n", "def feature(text: str) -> float:\n    'Count of specific substrings (e.g., \"GOATYilun\") in the text'\n    keyword = 'GOATYilun'\n    return float(text.count(keyword))\n\n", "def feature(text: str) -> float:\n    \"Number of distinct characters in the text\"\n    distinct_chars = set(text)\n    return float(len(distinct_chars))\n\n"], "all_features": ["def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    words = text.split()\n    unique_words = set(words)\n    return float(len(unique_words))\n\n", "def feature(text: str) -> float:\n    \"Ratio of uppercase letters to total characters in the text\"\n    uppercase_count = sum(1 for c in text if c.isupper())\n    return uppercase_count / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of numeric characters in the text divided by total length\"\n    numeric_count = sum(1 for c in text if c.isdigit())\n    return numeric_count / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Average sentence length (in words) in the text\"\n    sentence_count = text.count('.') + text.count('!') + text.count('?')\n    word_count = len(text.split())\n    return word_count / sentence_count if sentence_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Ratio of punctuation characters to total characters in the text\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return punctuation_count / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of repeating characters in the text (consecutive duplicates)\"\n    count = sum(1 for i in range(1, len(text)) if text[i] == text[i - 1])\n    return float(count)\n\n", "def feature(text: str) -> float:\n    \"Proportion of words longer than 5 characters in the text\"\n    words = text.split()\n    long_word_count = sum(1 for word in words if len(word) > 5)\n    return long_word_count / len(words) if len(words) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of sentences in the text (periods, exclamations, questions)\"\n    sentence_count = text.count('.') + text.count('!') + text.count('?')\n    return float(sentence_count)\n\n", "def feature(text: str) -> float:\n    \"Entropy of the character distribution in the text\"\n    from collections import Counter\n    import math\n    char_count = Counter(text)\n    total_chars = sum(char_count.values())\n    return -sum((count / total_chars) * math.log(count / total_chars, 2) for count in char_count.values() if count > 0)\n\n", "def feature(text: str) -> float:\n    \"Average length of the longest word in the text\"\n    words = text.split()\n    longest_word_length = max((len(word) for word in words), default=0)\n    return float(longest_word_length)\n", "def feature(text: str) -> float:\n    \"Proportion of uppercase letters in the text\"\n    uppercase_count = sum(1 for c in text if c.isupper())\n    if len(text) == 0:\n        return 0.0\n    return uppercase_count / len(text)\n\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    \"Average length of the sentences in the text\"\n    sentences = re.findall(r'[.!?]', text)\n    if len(sentences) == 0:\n        return 0.0\n    return len(text) / len(sentences)\n\n", "def feature(text: str) -> float:\n    \"Count of numerical digits in the text\"\n    digit_count = sum(1 for c in text if c.isdigit())\n    return float(digit_count)\n\n", "def feature(text: str) -> float:\n    \"Average number of punctuations per sentence in the text\"\n    sentence_count = text.count('.') + text.count('!') + text.count('?')\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    if sentence_count == 0:\n        return 0.0\n    return punctuation_count / sentence_count\n\n", "def feature(text: str) -> float:\n    \"Entropy of the character distribution in the text\"\n    from collections import Counter\n    import math\n    text_length = len(text)\n    if text_length == 0:\n        return 0.0\n    char_counts = Counter(text)\n    probabilities = [count / text_length for count in char_counts.values()]\n    return -sum(p * math.log(p, 2) for p in probabilities)\n\n", "def feature(text: str) -> float:\n    \"Frequency of the word 'detail' in the text\"\n    detail_count = text.lower().count('detail')\n    return float(detail_count)\n\n", "def feature(text: str) -> float:\n    \"Ratio of alphanumeric characters to total characters in the text\"\n    alphanumeric_count = sum(1 for c in text if c.isalnum())\n    if len(text) == 0:\n        return 0.0\n    return alphanumeric_count / len(text)\n\n", "def feature(text: str) -> float:\n    \"Count of words longer than 4 characters in the text\"\n    long_word_count = sum(1 for word in text.split() if len(word) > 4)\n    return float(long_word_count)\n\n", "def feature(text: str) -> float:\n    \"Ratio of specific keywords related to the context\"\n    keywords = ['AYilunGOT', 'GOATYilun', 'YilunGOAT', 'ZZZZZZZZZZ']\n    keyword_count = sum(1 for keyword in keywords if keyword in text)\n    return float(keyword_count)\n", "def feature(text: str) -> float:\n    \"Ratio of alphanumeric characters to total characters\"\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    alphanumeric_chars = sum(1 for c in text if c.isalnum())\n    return alphanumeric_chars / total_chars\n\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    \"Proportion of uppercase letters in the text\"\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    uppercase_count = sum(1 for c in text if c.isupper())\n    return uppercase_count / total_chars\n\n", "def feature(text: str) -> float:\n    \"Length of the longest word in the text\"\n    words = text.split()\n    if not words:\n        return 0.0\n    longest_word_length = max(len(word) for word in words)\n    return float(longest_word_length)\n\n", "def feature(text: str) -> float:\n    \"Total number of specific keywords (GOAT, Yilun) in the text\"\n    keywords = ['GOAT', 'Yilun']\n    keyword_count = sum(text.count(keyword) for keyword in keywords)\n    return float(keyword_count)\n\n", "def feature(text: str) -> float:\n    \"Average sentence length in words\"\n    sentences = re.findall(r'[.!?]', text)\n    word_count = len(text.split())\n    sentence_count = len(sentences)\n    if sentence_count == 0:\n        return 0.0\n    return float(word_count) / sentence_count\n\n", "def feature(text: str) -> float:\n    \"Proportion of punctuation characters in the text\"\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return punctuation_count / total_chars\n\n", "def feature(text: str) -> float:\n    \"Count of words containing digits in the text\"\n    words_with_digits = sum(1 for word in text.split() if any(c.isdigit() for c in word))\n    return float(words_with_digits)\n\n", "def feature(text: str) -> float:\n    \"Count of specific phrase occurrences in the text\"\n    phrase = 'YilunGOAT'\n    count = text.count(phrase)\n    return float(count)\n", "def feature(text: str) -> float:\n    'Entropy of character distribution in the text'\n    from collections import Counter\n    import math\n    if len(text) == 0:\n        return 0.0\n    char_counts = Counter(text)\n    probabilities = [count / len(text) for count in char_counts.values()]\n    entropy = -sum(p * math.log2(p) for p in probabilities)\n    return entropy\n\n", "def feature(text: str) -> float:\n    'Count of unique words in the text'\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    'Proportion of uppercase to total characters in the text'\n    upper_count = sum(1 for c in text if c.isupper())\n    total_count = len(text)\n    return upper_count / total_count if total_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    'Average sentence length in words'\n    sentences = re.findall(r'[.!?]', text)\n    words_count = len(text.split())\n    sentence_count = len(sentences)\n    return words_count / sentence_count if sentence_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    'Count of numeric digits in the text'\n    digit_count = sum(1 for c in text if c.isdigit())\n    return float(digit_count)\n\n", "def feature(text: str) -> float:\n    'Ratio of punctuation to total characters in the text'\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    total_count = len(text)\n    return punctuation_count / total_count if total_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    'Count of repeated phrases longer than one word'\n    words = text.split()\n    repeated_phrases = set()\n    for i in range(len(words)):\n        for j in range(i + 2, len(words) + 1):\n            phrase = ' '.join(words[i:j])\n            if text.count(phrase) > 1:\n                repeated_phrases.add(phrase)\n    return float(len(repeated_phrases))\n\n", "def feature(text: str) -> float:\n    'Ratio of long words to total words, where long is defined as > 5 characters'\n    words = text.split()\n    long_word_count = sum(1 for word in words if len(word) > 5)\n    return long_word_count / len(words) if words else 0.0\n\n", "def feature(text: str) -> float:\n    'Measure of text coherence using consecutive repeated words'\n    words = text.split()\n    repeat_count = sum(1 for i in range(len(words) - 1) if words[i] == words[i + 1])\n    return float(repeat_count)\n\n", "def feature(text: str) -> float:\n    'Count of specific keywords in the text related to the context'\n    keywords = ['answer', 'detail']\n    keyword_count = sum(text.lower().count(keyword) for keyword in keywords)\n    return float(keyword_count)\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    unique_words = set(text.split())\n    return float(len(unique_words))\n\n", "def feature(text: str) -> float:\n    \"Proportion of numeric characters to total characters\"\n    numeric_count = sum(c.isdigit() for c in text)\n    total_count = len(text)\n    return float(numeric_count) / total_count if total_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of words longer than 5 characters\"\n    long_words_count = sum(1 for word in text.split() if len(word) > 5)\n    return float(long_words_count)\n\n", "def feature(text: str) -> float:\n    \"Average sentence length in words\"\n    sentences = text.count('.') + text.count('!') + text.count('?')\n    words = len(text.split())\n    return float(words) / sentences if sentences > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Ratio of punctuation to total characters\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    total_count = len(text)\n    return float(punctuation_count) / total_count if total_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of repeated phrases of length 3 or more\"\n    words = text.split()\n    repeated_count = sum(1 for i in range(len(words) - 2) if words[i] == words[i+1] == words[i+2])\n    return float(repeated_count)\n\n", "def feature(text: str) -> float:\n    \"Density of capitalized words in the text\"\n    capitalized_count = sum(1 for word in text.split() if word[0].isupper())\n    total_words = len(text.split())\n    return float(capitalized_count) / total_words if total_words > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Entropy of character distribution in the text\"\n    from collections import Counter\n    from math import log2\n    text_length = len(text)\n    if text_length == 0:\n        return 0.0\n    char_count = Counter(text)\n    return -sum((count / text_length) * log2(count / text_length) for count in char_count.values())\n\n", "def feature(text: str) -> float:\n    \"Number of paragraphs based on line breaks\"\n    paragraphs = text.split('\\n')\n    return float(len([p for p in paragraphs if p.strip()]))\n\n", "def feature(text: str) -> float:\n    \"Ratio of content words (nouns, verbs, adjectives) to total words\"\n    content_word_count = sum(1 for word in text.split() if len(word) > 1 and word.lower() not in {'a', 'an', 'the', 'is', 'in', 'on', 'at'})\n    total_word_count = len(text.split())\n    return float(content_word_count) / total_word_count if total_word_count > 0 else 0.0\n", "def feature(text: str) -> float:\n    'Proportion of specific repeating characters in the text'\n    total_length = len(text)\n    if total_length == 0:\n        return 0.0\n    count_repeating = text.count('Z')\n    return count_repeating / total_length\n\n", "def feature(text: str) -> float:\n    'Count of unique words in the text'\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    'Average sentence length in words'\n    sentences = text.count('.') + text.count('!') + text.count('?')\n    if sentences == 0:\n        return 0.0\n    return len(text.split()) / sentences\n\n", "def feature(text: str) -> float:\n    'Density of the word \"detail\" in the text'\n    word_count = len(text.split())\n    if word_count == 0:\n        return 0.0\n    detail_count = text.lower().count('detail')\n    return detail_count / word_count\n\n", "def feature(text: str) -> float:\n    'Ratio of punctuation marks to total characters in the text'\n    total_length = len(text)\n    if total_length == 0:\n        return 0.0\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return punctuation_count / total_length\n\n", "def feature(text: str) -> float:\n    'Total count of characters that are digits'\n    return float(sum(1 for c in text if c.isdigit()))\n\n", "def feature(text: str) -> float:\n    'Count of uppercase letters versus total letters'\n    total_letters = sum(1 for c in text if c.isalpha())\n    if total_letters == 0:\n        return 0.0\n    uppercase_count = sum(1 for c in text if c.isupper())\n    return uppercase_count / total_letters\n\n", "def feature(text: str) -> float:\n    'Length of the longest word in the text'\n    words = text.split()\n    if not words:\n        return 0.0\n    return float(max(len(word) for word in words))\n\n", "def feature(text: str) -> float:\n    'Proportion of the text that consists of non-alphanumeric characters'\n    total_length = len(text)\n    if total_length == 0:\n        return 0.0\n    non_alphanumeric_count = sum(1 for c in text if not c.isalnum())\n    return non_alphanumeric_count / total_length\n", "def feature(text: str) -> float:\n    'Proportion of unique words in the text'\n    words = text.split()\n    unique_words = set(words)\n    if len(words) == 0:\n        return 0.0\n    return float(len(unique_words)) / len(words)\n\n", "def feature(text: str) -> float:\n    'Count of specific keywords related to the context'\n    keywords = ['ZZZZZZZZZ', 'detail']\n    count = sum(text.count(keyword) for keyword in keywords)\n    return float(count)\n\n", "def feature(text: str) -> float:\n    'Count of characters specific to a certain distribution'\n    return float(len([c for c in text if c in 'Zz']))\n\n", "def feature(text: str) -> float:\n    'Average sentence length in words'\n    sentences = len(re.findall(r'[.!?]', text))\n    words = len(text.split())\n    if sentences == 0:\n        return 0.0\n    return float(words) / sentences\n\n", "def feature(text: str) -> float:\n    'Ratio of alphanumeric characters to total characters'\n    alphanumeric_count = sum(c.isalnum() for c in text)\n    if len(text) == 0:\n        return 0.0\n    return float(alphanumeric_count) / len(text)\n\n", "def feature(text: str) -> float:\n    'Count of punctuation characters in the text'\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return float(punctuation_count)\n\n", "def feature(text: str) -> float:\n    'Proportion of uppercase letters in the text'\n    upper_count = sum(1 for c in text if c.isupper())\n    if len(text) == 0:\n        return 0.0\n    return float(upper_count) / len(text)\n\n", "def feature(text: str) -> float:\n    'Entropy of character distribution in the text'\n    from collections import Counter\n    if len(text) == 0:\n        return 0.0\n    freq = Counter(text)\n    probabilities = [count / len(text) for count in freq.values()]\n    return -sum(p * (p ** 0) for p in probabilities if p > 0)\n\n", "def feature(text: str) -> float:\n    'Percentage of the text that consists of whitespace'\n    whitespace_count = sum(1 for c in text if c.isspace())\n    if len(text) == 0:\n        return 0.0\n    return float(whitespace_count) / len(text)\n\n", "def feature(text: str) -> float:\n    'Count of distinct characters in the text'\n    distinct_characters = len(set(text))\n    return float(distinct_characters)\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    \"Ratio of uppercase letters to total letters in the text\"\n    uppercase_count = sum(1 for c in text if c.isupper())\n    total_count = sum(1 for c in text if c.isalpha())\n    if total_count == 0:\n        return 0.0\n    return float(uppercase_count) / total_count\n\n", "def feature(text: str) -> float:\n    \"Total number of punctuation marks in the text\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return float(punctuation_count)\n\n", "def feature(text: str) -> float:\n    \"Average sentence length in words\"\n    sentences = text.count('.') + text.count('!') + text.count('?')\n    words = len(text.split())\n    if sentences == 0:\n        return float(words)  # Treat as average length\n    return float(words) / sentences\n\n", "def feature(text: str) -> float:\n    \"Count of occurrences of the word 'detail'\"\n    return float(text.lower().count('detail'))\n\n", "def feature(text: str) -> float:\n    \"Length of the longest word in the text\"\n    words = text.split()\n    if not words:\n        return 0.0\n    longest_word_length = max(len(word) for word in words)\n    return float(longest_word_length)\n\n", "def feature(text: str) -> float:\n    \"Ratio of digits to total characters in the text\"\n    digit_count = sum(1 for c in text if c.isdigit())\n    total_count = len(text)\n    if total_count == 0:\n        return 0.0\n    return float(digit_count) / total_count\n\n", "def feature(text: str) -> float:\n    \"Proportion of words with more than 5 letters\"\n    words = text.split()\n    if not words:\n        return 0.0\n    long_words_count = sum(1 for word in words if len(word) > 5)\n    return float(long_words_count) / len(words)\n\n", "def feature(text: str) -> float:\n    \"Entropy of character distribution in the text\"\n    from collections import Counter\n    import math\n    char_count = Counter(text)\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    entropy = -sum((count / total_chars) * math.log(count / total_chars, 2) for count in char_count.values())\n    return float(entropy)\n\n", "def feature(text: str) -> float:\n    \"Sum of lengths of all unique words in the text\"\n    unique_words = set(text.split())\n    total_length = sum(len(word) for word in unique_words)\n    return float(total_length)\n", "def feature(text: str) -> float:\n    \"Entropy of the word lengths in the text\"\n    words = text.split()\n    if not words:\n        return 0.0\n    import math\n    length_counts = {}\n    for word in words:\n        length_counts[len(word)] = length_counts.get(len(word), 0) + 1\n    total_words = len(words)\n    entropy = -sum((count / total_words) * math.log(count / total_words) for count in length_counts.values())\n    return entropy\n\n", "def feature(text: str) -> float:\n    \"Ratio of the most frequent character to the total number of characters\"\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    char_counts = {}\n    for char in text:\n        if char.isalnum():\n            char_counts[char] = char_counts.get(char, 0) + 1\n    most_frequent = max(char_counts.values(), default=0)\n    return most_frequent / total_chars\n\n", "def feature(text: str) -> float:\n    \"Average sentence length in terms of word count\"\n    sentences = re.findall(r'[.!?]', text)\n    if not sentences:\n        return 0.0\n    word_count = len(text.split())\n    return word_count / len(sentences)\n\n", "def feature(text: str) -> float:\n    \"Count of distinct words in the text\"\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    \"Proportion of uppercase letters in the text\"\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    uppercase_count = sum(1 for c in text if c.isupper())\n    return uppercase_count / total_chars\n\n", "def feature(text: str) -> float:\n    \"Frequency of digits in the text\"\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    digit_count = sum(1 for c in text if c.isdigit())\n    return digit_count / total_chars\n\n", "def feature(text: str) -> float:\n    \"Average syllable count per word in the text\"\n    def syllable_count(word):\n        return len(re.findall(r'[aeiouy]{1,2}', word.lower()))\n\n    words = text.split()\n    if not words:\n        return 0.0\n    total_syllables = sum(syllable_count(word) for word in words)\n    return total_syllables / len(words)\n\n", "def feature(text: str) -> float:\n    \"Count of unique punctuation marks used in the text\"\n    punctuation_marks = set(c for c in text if not c.isalnum() and not c.isspace())\n    return float(len(punctuation_marks))\n\n", "def feature(text: str) -> float:\n    \"Proportion of sentences containing a specific keyword 'ZZZZZZZZZ'\"\n    sentences = re.split(r'[.!?]', text)\n    keyword = 'ZZZZZZZZZ'\n    if not sentences:\n        return 0.0\n    keyword_sentences = sum(1 for sentence in sentences if keyword in sentence)\n    return keyword_sentences / len(sentences)\n\n", "def feature(text: str) -> float:\n    \"Proportion of sentences that end with a question mark\"\n    sentences = re.findall(r'[.!?]', text)\n    if not sentences:\n        return 0.0\n    question_count = sentences.count('?')\n    return question_count / len(sentences)\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    words = text.split()\n    unique_words = len(set(words))\n    return float(unique_words)\n\n", "def feature(text: str) -> float:\n    \"Proportion of uppercase letters in the text\"\n    uppercase_count = sum(1 for c in text if c.isupper())\n    return uppercase_count / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of words that exceed a specified length\"\n    long_word_count = sum(1 for word in text.split() if len(word) > 5)\n    return float(long_word_count)\n\n", "def feature(text: str) -> float:\n    \"Density of numeric digits in the text\"\n    digit_count = sum(1 for c in text if c.isdigit())\n    return digit_count / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of specific repetitive phrases in the text\"\n    phrase_count = text.count('detail')\n    return float(phrase_count)\n\n", "def feature(text: str) -> float:\n    \"Measure of text 'boredom' based on repeated characters\"\n    repeated_char_count = sum(text.count(c) for c in set(text) if text.count(c) > 1)\n    return repeated_char_count / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Ratio of total punctuation marks to total characters\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return punctuation_count / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Average sentence length in words\"\n    sentences = len(re.findall(r'[.!?]', text)) or 1\n    word_count = len(text.split())\n    return float(word_count) / sentences\n\n", "def feature(text: str) -> float:\n    \"Count of words that are repeated more than twice\"\n    word_list = text.split()\n    repeated_word_count = sum(1 for word in set(word_list) if word_list.count(word) > 2)\n    return float(repeated_word_count)\n\n", "def feature(text: str) -> float:\n    \"Entropy of the character distribution in the text\"\n    from collections import Counter\n    char_count = Counter(text)\n    total_chars = sum(char_count.values())\n    return -sum((count / total_chars) * (len(char_count) - count / total_chars) for count in char_count.values()) if total_chars > 0 else 0.0\n", "def feature(text: str) -> float:\n    \"Calculate the ratio of uppercase letters to total letters\"\n    total_letters = sum(c.isalpha() for c in text)\n    if total_letters == 0:\n        return 0.0\n    uppercase_letters = sum(1 for c in text if c.isupper())\n    return uppercase_letters / total_letters\n\n", "def feature(text: str) -> float:\n    \"Measure the ratio of unique words to total words\"\n    words = text.split()\n    if not words:\n        return 0.0\n    unique_words = len(set(words))\n    return unique_words / len(words)\n\n", "def feature(text: str) -> float:\n    \"Calculate the total number of punctuation marks\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return float(punctuation_count)\n\n", "def feature(text: str) -> float:\n    \"Count the average number of characters per sentence\"\n    sentences = text.count('.') + text.count('!') + text.count('?')\n    if sentences == 0:\n        return 0.0\n    return len(text) / sentences\n\n", "def feature(text: str) -> float:\n    \"Calculate the entropy of character distribution in the text\"\n    from collections import Counter\n    import math\n    if not text:\n        return 0.0\n    char_count = Counter(text)\n    total_chars = len(text)\n    entropy = -sum((count / total_chars) * math.log(count / total_chars, 2) for count in char_count.values())\n    return entropy\n\n", "def feature(text: str) -> float:\n    \"Calculate the frequency of the word 'detail'\"\n    words = text.lower().split()\n    detail_count = words.count('detail')\n    return float(detail_count)\n\n", "def feature(text: str) -> float:\n    \"Measure the number of repeated characters\"\n    repeated_char_count = sum(1 for char in set(text) if text.count(char) > 1)\n    return float(repeated_char_count)\n\n", "def feature(text: str) -> float:\n    \"Calculate the length of the longest word in the text\"\n    words = text.split()\n    if not words:\n        return 0.0\n    return float(max(len(word) for word in words))\n\n", "def feature(text: str) -> float:\n    \"Calculate the proportion of numeric characters to total characters\"\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    numeric_count = sum(c.isdigit() for c in text)\n    return numeric_count / total_chars\n\n", "def feature(text: str) -> float:\n    \"Count the number of unique characters in the text\"\n    unique_chars = len(set(text))\n    return float(unique_chars)\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    unique_words = set(text.split())\n    return float(len(unique_words))\n\n", "def feature(text: str) -> float:\n    \"Ratio of uppercase letters to total letters in the text\"\n    total_letters = sum(1 for c in text if c.isalpha())\n    uppercase_letters = sum(1 for c in text if c.isupper())\n    return float(uppercase_letters) / total_letters if total_letters > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of all punctuation characters in the text\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return float(punctuation_count)\n\n", "def feature(text: str) -> float:\n    \"Proportion of numbers to total characters in the text\"\n    total_characters = len(text)\n    number_count = sum(1 for c in text if c.isdigit())\n    return float(number_count) / total_characters if total_characters > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Average length of sentences in the text\"\n    sentences = len(re.findall(r'[.!?]', text))\n    total_length = len(text)\n    return float(total_length) / sentences if sentences > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Frequency of the most common word in the text\"\n    words = text.split()\n    if not words:\n        return 0.0\n    most_common_word_count = max(words.count(word) for word in set(words))\n    return float(most_common_word_count)\n\n", "def feature(text: str) -> float:\n    \"Variance of word lengths in the text\"\n    words = text.split()\n    if not words:\n        return 0.0\n    mean_length = sum(len(word) for word in words) / len(words)\n    variance = sum((len(word) - mean_length) ** 2 for word in words) / len(words)\n    return variance\n\n", "def feature(text: str) -> float:\n    \"Count of consecutive repeating characters in the text\"\n    count = 0\n    for i in range(1, len(text)):\n        if text[i] == text[i - 1]:\n            count += 1\n    return float(count)\n\n", "def feature(text: str) -> float:\n    \"Ratio of the length of the longest word to the total number of words\"\n    words = text.split()\n    if not words:\n        return 0.0\n    longest_word_length = max(len(word) for word in words)\n    return float(longest_word_length) / len(words)\n\n", "def feature(text: str) -> float:\n    \"Density of specific keywords related to the context\"\n    keywords = ['answer', 'detail']\n    keyword_count = sum(text.lower().count(keyword) for keyword in keywords)\n    return float(keyword_count) / len(text.split()) if len(text.split()) > 0 else 0.0\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    words = text.split()\n    return float(len(set(words)))\n\n", "def feature(text: str) -> float:\n    \"Ratio of uppercase letters to total letters\"\n    total_letters = sum(1 for c in text if c.isalpha())\n    upper_letters = sum(1 for c in text if c.isupper())\n    return upper_letters / total_letters if total_letters > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Proportion of repeating phrases of more than two words\"\n    phrases = re.findall(r'(\\b\\w+\\b \\b\\w+\\b \\b\\w+\\b)', text)\n    repeated_phrases = len(set(phrase for phrase in phrases if phrases.count(phrase) > 1))\n    return repeated_phrases / len(phrases) if phrases else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of specific characters, e.g., 'Z', compared to total length\"\n    specific_char = 'Z'\n    count_specific = text.count(specific_char)\n    return count_specific / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Length of longest word in the text\"\n    words = text.split()\n    longest_word_length = max((len(word) for word in words), default=0)\n    return float(longest_word_length)\n\n", "def feature(text: str) -> float:\n    \"Proportion of sentences that contain a specific keyword (e.g., 'answer')\"\n    sentences = re.split(r'[.!?]', text)\n    keyword = 'answer'\n    keyword_sentences = sum(1 for sentence in sentences if keyword in sentence)\n    return keyword_sentences / len(sentences) if sentences else 0.0\n\n", "def feature(text: str) -> float:\n    \"Total punctuation count relative to text length\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return punctuation_count / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Average sentence length in words\"\n    sentences = re.split(r'[.!?]', text)\n    word_count = sum(len(sentence.split()) for sentence in sentences)\n    return word_count / len(sentences) if len(sentences) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Frequency of the most common word\"\n    words = text.split()\n    if not words:\n        return 0.0\n    most_common_word_count = max(words.count(word) for word in set(words))\n    return float(most_common_word_count) / len(words)\n\n", "def feature(text: str) -> float:\n    \"Count of conjunctions (and, but, or) relative to total words\"\n    conjunctions = ['and', 'but', 'or']\n    words = text.lower().split()\n    conjunction_count = sum(1 for word in words if word in conjunctions)\n    return conjunction_count / len(words) if words else 0.0\n", "def feature(text: str) -> float:\n    'Ratio of uppercase letters to total characters in the text'\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    uppercase_count = sum(1 for c in text if c.isupper())\n    return uppercase_count / total_chars\n\n", "def feature(text: str) -> float:\n    'Count of distinct words in the text'\n    words = text.split()\n    distinct_words = len(set(words))\n    return float(distinct_words)\n\n", "def feature(text: str) -> float:\n    'Average sentence length in terms of word count'\n    sentence_count = text.count('.') + text.count('!') + text.count('?')\n    if sentence_count == 0:\n        return 0.0\n    word_count = len(text.split())\n    return word_count / sentence_count\n\n", "def feature(text: str) -> float:\n    'Proportion of sentences that contain a keyword'\n    keyword = 'detail'\n    sentences = re.split(r'[.!?]', text)\n    keyword_count = sum(1 for sentence in sentences if keyword in sentence)\n    return float(keyword_count) / len(sentences) if sentences else 0.0\n\n", "def feature(text: str) -> float:\n    'Total number of punctuation marks in the text'\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return float(punctuation_count)\n\n", "def feature(text: str) -> float:\n    'Ratio of numeric characters to total characters in the text'\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    numeric_count = sum(1 for c in text if c.isdigit())\n    return numeric_count / total_chars\n\n", "def feature(text: str) -> float:\n    'Count of specific substrings (e.g., \"GOATYilun\") in the text'\n    keyword = 'GOATYilun'\n    return float(text.count(keyword))\n\n", "def feature(text: str) -> float:\n    'Variance of word lengths in the text'\n    words = text.split()\n    if not words:\n        return 0.0\n    word_lengths = [len(word) for word in words]\n    mean_length = sum(word_lengths) / len(words)\n    variance = sum((length - mean_length) ** 2 for length in word_lengths) / len(words)\n    return variance\n\n", "def feature(text: str) -> float:\n    'Proportion of words that are repeated in the text'\n    words = text.split()\n    total_words = len(words)\n    if total_words == 0:\n        return 0.0\n    word_counts = {}\n    for word in words:\n        word_counts[word] = word_counts.get(word, 0) + 1\n    repeated_words = sum(1 for count in word_counts.values() if count > 1)\n    return repeated_words / total_words\n\n", "def feature(text: str) -> float:\n    'Number of words in the longest sentence in the text'\n    sentences = re.split(r'[.!?]', text)\n    longest_sentence_length = max(len(sentence.split()) for sentence in sentences) if sentences else 0\n    return float(longest_sentence_length)\n", "def feature(text: str) -> float:\n    'Count of unique words in the text'\n    words = text.split()\n    unique_words = set(words)\n    return float(len(unique_words))\n\n", "def feature(text: str) -> float:\n    'Proportion of uppercase letters to total letters in the text'\n    uppercase_count = sum(1 for c in text if c.isupper())\n    total_letters = sum(1 for c in text if c.isalpha())\n    return float(uppercase_count) / total_letters if total_letters > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    'Number of punctuations divided by word count'\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    word_count = len(text.split())\n    return float(punctuation_count) / word_count if word_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    'Count of specific recurring words to total words'\n    target_words = ['detail', 'answer']\n    total_count = len(text.split())\n    target_count = sum(text.lower().count(word) for word in target_words)\n    return float(target_count) / total_count if total_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    'Ratio of long words (more than 7 characters) to total words'\n    long_word_count = sum(1 for word in text.split() if len(word) > 7)\n    total_count = len(text.split())\n    return float(long_word_count) / total_count if total_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    'Average sentence length (words per sentence)'\n    sentence_count = text.count('.') + text.count('!') + text.count('?')\n    word_count = len(text.split())\n    return float(word_count) / sentence_count if sentence_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    'Entropy of character distribution in the text'\n    import math\n    char_count = len(text)\n    if char_count == 0:\n        return 0.0\n    frequency = {}\n    for char in text:\n        if char.isalnum():\n            frequency[char] = frequency.get(char, 0) + 1\n    entropy = -sum((count / char_count) * math.log(count / char_count, 2) for count in frequency.values())\n    return entropy\n\n", "def feature(text: str) -> float:\n    'Ratio of numbers to total words'\n    number_count = sum(1 for word in text.split() if word.isdigit())\n    total_count = len(text.split())\n    return float(number_count) / total_count if total_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    'Count of sentences containing certain keywords'\n    keywords = ['ZZZZZZZZZ']\n    sentence_count = text.count('.') + text.count('!') + text.count('?')\n    matching_sentences = sum(1 for sentence in text.split('.') if any(keyword in sentence for keyword in keywords))\n    return float(matching_sentences) / sentence_count if sentence_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    'Average length of unique words in the text'\n    words = text.split()\n    unique_words = set(words)\n    if not unique_words:\n        return 0.0\n    return sum(len(word) for word in unique_words) / len(unique_words)\n", "def feature(text: str) -> float:\n    \"Proportion of uppercase letters in the text\"\n    uppercase_count = sum(1 for c in text if c.isupper())\n    total_length = len(text)\n    return uppercase_count / total_length if total_length else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    \"Ratio of numeric characters to total characters in the text\"\n    digit_count = sum(1 for c in text if c.isdigit())\n    total_length = len(text)\n    return digit_count / total_length if total_length else 0.0\n\n", "def feature(text: str) -> float:\n    \"Average sentence length in words\"\n    sentences = text.count('.') + text.count('!') + text.count('?')\n    word_count = len(text.split())\n    return word_count / sentences if sentences else 0.0\n\n", "def feature(text: str) -> float:\n    \"Density of specific keywords in the text\"\n    keywords = ['detail', 'answer']  # Example keywords\n    keyword_count = sum(text.lower().count(keyword) for keyword in keywords)\n    total_length = len(text.split())\n    return keyword_count / total_length if total_length else 0.0\n\n", "def feature(text: str) -> float:\n    \"Frequency of exclamation and question marks\"\n    punctuation_count = text.count('!') + text.count('?')\n    total_length = len(text)\n    return punctuation_count / total_length if total_length else 0.0\n\n", "def feature(text: str) -> float:\n    \"Length of the longest word in the text\"\n    words = text.split()\n    return float(max(len(word) for word in words)) if words else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of punctuation characters in the text\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return float(punctuation_count)\n\n", "def feature(text: str) -> float:\n    \"Count of repeated phrases in the text\"\n    words = text.split()\n    phrase_count = sum(1 for i in range(len(words) - 1) if words[i] == words[i + 1])\n    return float(phrase_count)\n\n", "def feature(text: str) -> float:\n    \"Entropy of the character distribution\"\n    from collections import Counter\n    import math\n    char_count = Counter(text)\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    return -sum((count / total_chars) * math.log(count / total_chars, 2) for count in char_count.values())\n", "def feature(text: str) -> float:\n    \"Proportion of uppercase letters in the text\"\n    uppercase_count = sum(1 for c in text if c.isupper())\n    return uppercase_count / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Total number of punctuation marks in the text\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return float(punctuation_count)\n\n", "def feature(text: str) -> float:\n    \"Average length of punctuated sentences in the text\"\n    sentences = re.findall(r'[^.!?]+[.!?]', text)\n    if not sentences:\n        return 0.0\n    return sum(len(sentence) for sentence in sentences) / len(sentences)\n\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    \"Ratio of numeric characters to total characters in the text\"\n    digit_count = sum(1 for c in text if c.isdigit())\n    return digit_count / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Length of the longest word in the text\"\n    words = text.split()\n    return float(max(len(word) for word in words)) if words else 0.0\n\n", "def feature(text: str) -> float:\n    \"Entropy of character distribution in the text\"\n    from collections import Counter\n    import math\n    length = len(text)\n    if length == 0:\n        return 0.0\n    freq = Counter(text)\n    return -sum((count / length) * math.log(count / length, 2) for count in freq.values())\n\n", "def feature(text: str) -> float:\n    \"Ratio of palindrome words to total words in the text\"\n    words = text.split()\n    palindrome_count = sum(1 for word in words if word == word[::-1])\n    return palindrome_count / len(words) if words else 0.0\n\n", "def feature(text: str) -> float:\n    \"Average sentence length measured in words\"\n    sentences = re.findall(r'[^.!?]+[.!?]', text)\n    if not sentences:\n        return 0.0\n    return sum(len(sentence.split()) for sentence in sentences) / len(sentences)\n\n", "def feature(text: str) -> float:\n    \"Number of repeated consecutive characters in the text\"\n    return float(len(re.findall(r'(.)\\1+', text)))\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    \"Proportion of capitalized words to total words in the text\"\n    words = text.split()\n    if not words:\n        return 0.0\n    capitalized = sum(1 for word in words if word[0].isupper())\n    return float(capitalized) / len(words)\n\n", "def feature(text: str) -> float:\n    \"Average sentence length in words\"\n    sentences = len(re.findall(r'[.!?]', text))\n    words = len(text.split())\n    if sentences == 0:\n        return 0.0\n    return float(words) / sentences\n\n", "def feature(text: str) -> float:\n    \"Standard deviation of word lengths\"\n    words = text.split()\n    if not words:\n        return 0.0\n    lengths = [len(word) for word in words]\n    mean = sum(lengths) / len(lengths)\n    variance = sum((x - mean) ** 2 for x in lengths) / len(lengths)\n    return variance ** 0.5\n\n", "def feature(text: str) -> float:\n    \"Count of punctuation marks in the text\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return float(punctuation_count)\n\n", "def feature(text: str) -> float:\n    \"Ratio of stopwords to total words in the text\"\n    stopwords = {'the', 'is', 'in', 'and', 'to', 'of', 'a', 'that', 'it', 'for', 'on', 'with'}\n    words = text.split()\n    if not words:\n        return 0.0\n    stopword_count = sum(1 for word in words if word.lower() in stopwords)\n    return float(stopword_count) / len(words)\n\n", "def feature(text: str) -> float:\n    \"Ratio of the length of the longest word to the total length of the text\"\n    words = text.split()\n    if not words:\n        return 0.0\n    longest_word_length = max(len(word) for word in words)\n    total_length = len(text)\n    return float(longest_word_length) / total_length\n\n", "def feature(text: str) -> float:\n    \"Frequency of the most common word in the text\"\n    from collections import Counter\n    words = text.split()\n    if not words:\n        return 0.0\n    word_count = Counter(words)\n    most_common_count = word_count.most_common(1)[0][1]\n    return float(most_common_count) / len(words)\n\n", "def feature(text: str) -> float:\n    \"Count of numeric digits in the text\"\n    digit_count = sum(1 for c in text if c.isdigit())\n    return float(digit_count)\n", "def feature(text: str) -> float:\n    \"Count of digits in the text\"\n    return float(sum(1 for c in text if c.isdigit()))\n\n", "def feature(text: str) -> float:\n    \"Proportion of uppercase letters in the text\"\n    uppercase_count = sum(1 for c in text if c.isupper())\n    return float(uppercase_count) / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Length of the longest word in the text\"\n    words = text.split()\n    return float(max(len(word) for word in words)) if words else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of specific keyword 'detail' in the text\"\n    return float(text.lower().count('detail'))\n\n", "def feature(text: str) -> float:\n    \"Average sentence length measured in words\"\n    sentences = len(re.findall(r'[.!?]', text)) + 1\n    word_count = len(text.split())\n    return float(word_count) / sentences if sentences > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Ratio of punctuation marks to total length of text\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return float(punctuation_count) / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of unique words in the text\"\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    \"Density of specific substring 'AYilunGOT' in the text\"\n    return float(text.count('AYilunGOT')) / len(text) if len(text) > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Average length of unique words in the text\"\n    words = set(text.split())\n    return float(sum(len(word) for word in words)) / len(words) if words else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count of occurrences of the word 'answer' in the text\"\n    return float(text.lower().count('answer'))\n", "def feature(text: str) -> float:\n    \"Entropy of the character distribution in the text\"\n    from collections import Counter\n    import math\n    char_count = Counter(text)\n    total_chars = len(text)\n    if total_chars == 0:\n        return 0.0\n    entropy = -sum((count / total_chars) * math.log(count / total_chars, 2) for count in char_count.values())\n    return entropy\n\n", "def feature(text: str) -> float:\n    \"Ratio of uppercase letters to total letters in the text\"\n    total_letters = sum(1 for c in text if c.isalpha())\n    if total_letters == 0:\n        return 0.0\n    uppercase_count = sum(1 for c in text if c.isupper())\n    return uppercase_count / total_letters\n\n", "def feature(text: str) -> float:\n    \"Count of distinct words in the text\"\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    \"Average length of consecutive repeated phrases in the text\"\n    import re\n    phrases = re.findall(r'(\\b\\w+\\b)(?:.*?\\1)+', text)\n    if not phrases:\n        return 0.0\n    return sum(len(phrase.split()) for phrase in phrases) / len(phrases)\n\n", "def feature(text: str) -> float:\n    \"Frequency of the substring 'GOATYilun' in the text\"\n    return float(text.count('GOATYilun'))\n\n", "def feature(text: str) -> float:\n    \"Length of the longest word in the text\"\n    words = text.split()\n    if not words:\n        return 0.0\n    return float(max(len(word) for word in words))\n\n", "def feature(text: str) -> float:\n    \"Proportion of sentences that contain the substring 'GOATYilun'\"\n    sentences = re.split(r'[.!?]', text)\n    if len(sentences) == 0:\n        return 0.0\n    count_goaty = sum(1 for sentence in sentences if 'GOATYilun' in sentence)\n    return count_goaty / len(sentences)\n\n", "def feature(text: str) -> float:\n    \"Average sentence length in words\"\n    sentences = re.split(r'[.!?]', text)\n    total_words = len(text.split())\n    num_sentences = len([s for s in sentences if s.strip()])\n    if num_sentences == 0:\n        return 0.0\n    return total_words / num_sentences\n\n", "def feature(text: str) -> float:\n    \"Ratio of punctuation marks to the number of words in the text\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    if len(text.split()) == 0:\n        return 0.0\n    return punctuation_count / len(text.split())\n", "def feature(text: str) -> float:\n    \"Count occurrences of specific capitalized keywords.\"\n    keywords = ['YilunGOAT']\n    return float(sum(text.count(keyword) for keyword in keywords))\n\n", "def feature(text: str) -> float:\n    \"Ratio of uppercase letters to total letters in the text.\"\n    upper_count = sum(1 for c in text if c.isupper())\n    total_count = len(text)\n    return upper_count / total_count if total_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Measure the average length of the sentences in the text.\"\n    sentences = text.count('.') + text.count('!') + text.count('?')\n    words = len(text.split())\n    return words / sentences if sentences > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count the number of unique words in the text.\"\n    words = set(text.split())\n    return float(len(words))\n\n", "def feature(text: str) -> float:\n    \"Calculate the proportion of non-alphanumeric characters in the text.\"\n    non_alnum_count = sum(1 for c in text if not c.isalnum())\n    total_count = len(text)\n    return non_alnum_count / total_count if total_count > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count how many times the phrase 'detail' appears in the text.\"\n    return float(text.lower().count('detail'))\n\n", "def feature(text: str) -> float:\n    \"Measure the length of the longest word in the text.\"\n    words = text.split()\n    return float(max(len(word) for word in words)) if words else 0.0\n\n", "def feature(text: str) -> float:\n    \"Calculate the ratio of sentences to total words in the text.\"\n    sentences = text.count('.') + text.count('!') + text.count('?')\n    words = len(text.split())\n    return sentences / words if words > 0 else 0.0\n\n", "def feature(text: str) -> float:\n    \"Count the number of times the text ends with a punctuation mark.\"\n    return float(text.endswith('.') or text.endswith('!') or text.endswith('?'))\n\n", "def feature(text: str) -> float:\n    \"Average character length of all words in the text.\"\n    words = text.split()\n    return float(sum(len(word) for word in words)) / len(words) if words else 0.0\n", "def feature(text: str) -> float:\n    \"Proportion of unique words in the text\"\n    words = text.split()\n    unique_words = set(words)\n    if len(words) == 0:\n        return 0.0\n    return float(len(unique_words)) / len(words)\n\n", "def feature(text: str) -> float:\n    \"Count of words appearing more than once in the text\"\n    words = text.split()\n    word_counts = {}\n    for word in words:\n        word_counts[word] = word_counts.get(word, 0) + 1\n    return float(sum(1 for count in word_counts.values() if count > 1))\n\n", "def feature(text: str) -> float:\n    \"Number of distinct characters in the text\"\n    distinct_chars = set(text)\n    return float(len(distinct_chars))\n\n", "def feature(text: str) -> float:\n    \"Count of non-text characters (digits, punctuation) in the text\"\n    non_text_count = sum(1 for c in text if not c.isalpha() and not c.isspace())\n    return float(non_text_count)\n\n", "def feature(text: str) -> float:\n    \"Ratio of the longest word's length to the total character count\"\n    words = text.split()\n    if not words:\n        return 0.0\n    longest_word_length = max(len(word) for word in words)\n    return float(longest_word_length) / len(text)\n\n", "def feature(text: str) -> float:\n    \"Number of sentences in the text divided by word count\"\n    sentence_count = text.count('.') + text.count('!') + text.count('?')\n    word_count = len(text.split())\n    if word_count == 0:\n        return 0.0\n    return float(sentence_count) / word_count\n\n", "def feature(text: str) -> float:\n    \"Average sentence length in words\"\n    sentence_count = text.count('.') + text.count('!') + text.count('?')\n    word_count = len(text.split())\n    if sentence_count == 0:\n        return 0.0\n    return float(word_count) / sentence_count\n\n", "def feature(text: str) -> float:\n    \"Punctuation frequency in the text\"\n    punctuation_count = sum(1 for c in text if not c.isalnum() and not c.isspace())\n    return float(punctuation_count) / len(text)\n\n", "def feature(text: str) -> float:\n    \"Count of capitalized words in the text\"\n    capitalized_count = sum(1 for word in text.split() if word[0].isupper())\n    return float(capitalized_count)\n\n", "def feature(text: str) -> float:\n    \"Ratio of vowels to total letters in the text\"\n    vowels = 'aeiouAEIOU'\n    vowel_count = sum(1 for c in text if c in vowels)\n    letter_count = sum(1 for c in text if c.isalpha())\n    if letter_count == 0:\n        return 0.0\n    return float(vowel_count) / letter_count\n"]}